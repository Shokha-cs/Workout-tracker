// import { render, screen } from "@testing-library/react";
// import App from "./App";

// test("renders backend message heading", () => {
//   render(<App />);
//   const headingElement = screen.getByText(/backend message:/i);
//   expect(headingElement).toBeInTheDocument();
// });
